apply plugin: 'java'
apply plugin: 'eclipse'

version = '1.0'

repositories {
  mavenCentral()
}

dependencies {
  testCompile "junit:junit:4.11"
}

sourceSets {
  main {
    java {
      srcDir "core/src"
    }
  }
  test {
    java {
      srcDir "core/test"
    }
  }
}

